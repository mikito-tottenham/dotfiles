interface:
  display_name: "Gemini CLI Runner"
  short_description: "Run Gemini CLI with observable artifacts"
  default_prompt: "Use $gemini-cli-runner to run a Gemini CLI subprocess with stream-json logs, timeout guards, and failure artifacts."
