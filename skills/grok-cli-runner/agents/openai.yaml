interface:
  display_name: "Grok CLI Runner"
  short_description: "Call Grok and inspect X URLs"
  default_prompt: "Use $grok-cli-runner to run a Grok handoff through Homebrew-managed Hermes/SuperGrok OAuth by default, preserving request/response artifacts, direct X Search engagement artifacts, timeout guards, and failure summaries."
